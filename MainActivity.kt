// un cliente WebSocket para conectarte al servidor
class ChatClient {
    private val client = OkHttpClient()

    fun sendMessage(message: String) {
        // Lógica para enviar un mensaje a través de WebSocket
    }
}
